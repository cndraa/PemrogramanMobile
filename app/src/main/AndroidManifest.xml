<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tugasprogmob">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings"></activity>
        <activity android:name=".DialogActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".RecyclerViewActivity" />
        <activity android:name=".AddMhsActivity" />
        <activity android:name=".UpdateMhsActivity" />
        <activity android:name=".ListMhsActivity" />
        <activity android:name=".ListActivity" />
        <activity android:name=".MahasiswaActivity" />
        <activity android:name=".FragmenActivity" />
        <activity android:name=".Main3Activity" />
        <activity android:name=".Main2Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HelpActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>